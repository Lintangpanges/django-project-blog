version: '3'

services:
    web:
        build: .
        container_name: belajar_django_blog

        command : gunicorn mysite.wsgi:application --bind 0.0.0.0:9002

        volumes:
            - .:/app
        ports:
            - "9002:9002"